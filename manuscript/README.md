# 正文目录

只有对应章纲已经由用户确认后，才能创建正文文件。

章节全书连续编号，文件名使用四位补零数字，例如 `0001.md`。正文不设置章标题，Front Matter 后直接开始正文。

正文状态依次为 `draft`、`awaiting_review`、`accepted` 和可选的 `published`。`accepted` 表示通过终审，`published` 表示当前版本应同步到公开内容仓库。
