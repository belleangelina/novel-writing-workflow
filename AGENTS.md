# 长篇小说仓库 Agent 规则

## 1. 权限边界

本仓库用于个人作者与 Coding Agent 协作创作长篇小说。你负责执行创作流程，不负责替用户决定创作方法。

用户拥有最高权威：

- 用户的当前指令优先于仓库中的默认流程。
- 用户直接修改的正文立即视为权威文本。
- 未经用户确认，不得修改本文件、目录结构、文风指南或既有正式设定。
- 不得把猜测写成事实；不确定且无法从仓库核实时，明确说明并询问用户。

`AGENTS.md` 是本仓库的行为规范。

## 2. 开始工作前

每次工作前必须：

1. 检查 Git 状态和现有差异。
2. 读取 `workflow/state.yaml`。
3. 识别用户未提交的修改并完整保留。
4. 根据当前阶段装载规定的上下文。

不得覆盖、回退、暂存或提交用户未明确授权处理的修改。任务与用户修改重叠时，以用户修改后的文件为基础；无法判断意图时，停止相关编辑并询问。

## 3. 唯一权威来源

| 问题 | 权威来源 |
|---|---|
| 故事准备怎样发展 | `outlines/` |
| 世界中长期有效的事实 | `canon/` |
| 当前时间线、人物状态和未解线索 | `continuity/` |
| 实际已经写出的文本 | `manuscript/` |
| 当前工作阶段和轮次 | `workflow/state.yaml` |
| 当前文风约束 | `style/guide.md` |
| 逐句文风审查方法 | `workflow/prose-review/review-guide.md` |
| 用户确认过什么 | 文件 `status` 与 Git 确认点提交 |

来源之间冲突时不得自行选择。先指出冲突，再请求用户裁决。

连续性文件职责如下：

- `continuity/current-state.md` 只保存截至最新连续通过章节的简洁状态快照，包括人物当前掌握和不知道的信息；
- `continuity/timeline.md` 保存会影响后续判断的关键事件及时间关系；
- `continuity/threads.md` 保存尚未解决的悬念、承诺和伏笔，解决后应移出未解列表或明确标记已解决。

## 4. 文件与状态

章节全书连续编号，文件名使用四位补零数字，不使用章标题：

```text
outlines/chapters/0042.md
manuscript/0042.md
```

YAML Front Matter 是作品结构、卷号、章号和审批状态的权威来源。文件名与 Front Matter 不一致时，报告问题，不得静默修复。

新建文件时使用以下最小 Front Matter：

```yaml
# 总纲
structure: undecided
status: draft

# 卷纲
volume: 1
status: draft

# 章纲
volume: 1
chapter: 1
pov: 待确认
status: draft

# 正文
volume: 1
chapter: 1
status: draft
```

以上片段分别放入各自文件，不合并成一个 YAML 块，也不增加没有实际用途的预留字段。

总纲的 `structure` 只允许：

- `undecided`：初始化时尚未确认；
- `single_volume`：单卷作品，总纲同时承担卷纲职责；
- `multi_volume`：多卷作品，使用独立卷纲。

总纲提交用户确认前，`structure` 必须已经由用户决定，不得保留 `undecided`。

大纲状态只允许：

- `draft`；
- `awaiting_approval`；
- `approved`。

正文状态只允许：

- `draft`；
- `awaiting_review`；
- `accepted`；
- `published`。

`accepted` 表示正文已经通过用户终审，但尚未授权公开；`published` 表示创作仓库中的当前版本应与公开副本保持一致。只有用户明确要求发布，才能把正文改为 `published`。

文风指南状态只允许 `draft` 或 `approved`。只有用户明确确认后，才能写入 `approved` 或 `accepted`。

`workflow/state.yaml` 的 `phase` 只允许：

- `initialization`；
- `outline_drafting`；
- `outline_logic_review`；
- `awaiting_outline_approval`；
- `prose_drafting`；
- `prose_logic_review`；
- `prose_style_review`；
- `prose_continuity_review`；
- `awaiting_final_review`；
- `escalated`；
- `idle`。

批量规划章纲时，`current_chapter` 表示当前正在处理的章号；整批等待确认时，记录该批最早的章号。它不表示已经获准进入该章正文。

`round` 只表示当前审查阶段的轮次。进入审查时从 `1` 开始，同类复审时递增，切换到下一类审查时重置为 `1`，非审查阶段为 `0`。文件 `status` 表示产物审批状态，流程状态表示当前动作，两者不得互相替代。

阶段必须随工作推进：

- 起草大纲时使用 `outline_drafting`，审查时使用 `outline_logic_review`；
- 本轮规划范围内的大纲全部审查通过后，把对应大纲状态改为 `awaiting_approval`，阶段改为 `awaiting_outline_approval`；
- 用户确认章纲后，把对应章纲状态改为 `approved`；下一项是正文时进入 `prose_drafting`，仍要继续规划章纲时进入 `outline_drafting`；
- 正文依次进入 `prose_logic_review`、`prose_style_review`、`prose_continuity_review`；
- 所有正文审查通过后，把正文状态改为 `awaiting_review`，阶段改为 `awaiting_final_review`；
- 无法在三轮内通过时使用 `escalated`；
- 用户终审通过并完成同步与确认点提交后，进入下一项明确任务；没有下一项时使用 `idle`。

## 5. 标准上下文

设当前章节为 `N`，所属卷为 `V`。创作或审查正文时必须读取：

- 本文件；
- `outlines/overall.md`；
- 当前章 `N` 的已确认章纲；
- 已确认的 `style/guide.md`。
- `workflow/prose-review/review-guide.md`。

总纲的 `structure` 为 `multi_volume` 时，还必须读取当前卷 `V` 的已确认卷纲；为 `single_volume` 时，总纲同时承担卷纲职责，不读取独立卷纲。

以下文件存在则读取，不存在就跳过，不用更远内容补位：

- `N-1`、`N+1` 章纲；
- `N-1`、`N`、`N+1` 正文；
- 已读取章纲直接链接的设定和连续性文件。

多卷作品还读取存在的 `V-1`、`V+1` 卷纲，并固定采用 `V-1 / V / V+1` 三卷窗口。单卷作品不使用卷纲窗口。所有作品的章纲和正文固定采用 `N-1 / N / N+1` 三章窗口。

标准窗口不是事实核实的上限。存在明确远程引用、疑似冲突或伏笔回收问题时，可以定向读取确切来源。额外读取必须服务于明确问题，并记录到交付摘要；不得无目的扫描整部小说。

## 6. 初始化流程

`workflow/state.yaml` 为 `initialization` 时，依次执行：

```text
与用户头脑风暴
→ 起草、审查并确认文风指南
→ 起草、审查并确认总纲及单卷/多卷结构
→ 按作品结构分支
  ├─ 多卷：起草、审查并确认当前卷卷纲
  └─ 单卷：由总纲兼任卷纲
→ 与用户确定章纲规划范围
→ 起草、审查并确认所选的一章、连续若干章或全书章纲
→ 按用户要求继续规划章纲或进入正文流程
```

章纲规划范围不由模板固定，可以是一章、连续若干章或全书；用户可以随时调整尚未确认部分的范围和分章。任何正文开始前，文风指南、总纲和对应章纲都必须已确认；多卷作品还必须确认正文所属卷的卷纲。

## 7. 大纲规则

总纲必须非常简洁，讲清整部小说的核心冲突、总体演变和结局方向。

单卷作品由总纲同时承担卷纲职责，不创建独立卷纲；模板自带的卷纲占位文件应在确认 `single_volume` 时删除。多卷作品的卷纲必须简洁，讲清本卷如何推动总纲、主要转折和卷末落点。

章纲必须详细，但不使用固定栏目或填表结构。使用自然叙述理清事件先后、持续时间、人物位置与移动、场景转换、行动因果、信息状态，以及与前后章的关系。章纲通过普通 Markdown 链接引用相关设定与连续性文件。

连续规划多章时，按章节顺序起草，既分别检查每章内部逻辑，也检查相邻章的分界、承接、重复事件和信息流。可以整批交付用户确认，不要求确认一章后立即写该章正文。多卷作品只有在相关卷纲已经确认后，才能继续规划该卷章纲。

章纲 Front Matter 的 `pov` 表示本章视角人物或视角类型。单一视角直接填写人物名；全知视角填写 `全知`；一章包含多个视角时填写 `多视角`，并在章纲自然叙述中明确每个场景的视角及切换位置。

所有大纲都做逻辑审查，不做逐句文风审查。未经用户确认的章纲不得用于创作正文。

## 8. 审查通用规则

每次审查只能得出一种结论：

- `PASS`：没有需要修改的确切问题；
- `REVISE`：存在可定位、可说明依据的问题；
- `ESCALATE`：存在无法自行裁决的创作分歧。

只有设定冲突、因果或时空错误、信息状态错误、明确违反已确认文风等问题可以阻止通过。主观的“另一种写法可能更好”只能作为非阻断建议，不得触发自动重写。

逐句文风判断以已确认的 `style/guide.md` 为准，检查方法以 `workflow/prose-review/review-guide.md` 为准。`workflow/prose-review/experiments/` 中的工具分数和历史结果只作为证据，不能单独构成 `PASS`、`REVISE` 或写入正式正文的依据。

`REVISE` 后先修改，再重新执行同一种审查。每类审查最多三轮；第三轮仍不能 `PASS` 时进入 `ESCALATE`，保留当前最佳版本并向用户提交问题、依据和备选方案。

审查和修改必须是两个可区分的步骤。完整中间问题清单放入 `workspace/`，最终只保留摘要。

## 9. 章纲流程

开始前先与用户确定本轮范围：一章、连续若干章或全书。批量规划不降低单章章纲的详细程度。

```text
与用户讨论范围、分章和关键走向
→ 按顺序起草范围内的详细章纲
→ 逐章审查逻辑、时间与空间关系
→ 审查相邻章节的分界与承接
→ 修改并复审，最多三轮
→ 将一章或整批章纲交付用户确认
```

批量模式下，某章通过内部审查后可以保持 `awaiting_approval`，同时继续起草后续章纲；此时流程阶段表示 Agent 正在执行的动作，不要求停在等待状态。整批准备完毕后再进入 `awaiting_outline_approval`。

用户确认前，审查通过的章纲状态必须为 `awaiting_approval`，不得提前写成 `approved`。用户确认后把对应章纲状态改为 `approved`；如果规划范围尚未完成，继续章纲流程，如果下一项是正文，再进入正文阶段。

## 10. 正文流程

场景是 Agent 内部生产单位，章节是用户交付单位。场景不要求成为独立最终文件。

```text
按场景编写
→ 章内逻辑审查 ↔ 修改
→ 逐句文风审查 ↔ 修改
→ 跨章衔接审查 ↔ 修改
→ 对受影响部分定向回归
→ 整章交付用户终审
```

逐句文风审查必须按 `workflow/prose-review/review-guide.md` 执行，是否合格只依据已确认的 `style/guide.md`。模板文风指南提供可修改的默认预设；项目初始化时可以修改，确认后以项目版本为准。

正文写完后必须从头到尾逐句连读，不能只检查情节。判断人物声音时可以参考已经通过终审的相邻正文；使用外部检测器时直接遵循审查指南中的受控实验流程。逐句检查不要求每句留下记录，但交付摘要要写明发现并改掉了哪些问题。

章内逻辑审查必须依据章纲 `pov` 和 `continuity/current-state.md` 检查视角人物是否接触过相关信息。逐句文风审查必须依据 `style/guide.md` 检查视角越界、无意的视角漂移和不符合规则的切换。

跨章衔接审查至少检查人物位置、时间、伤势、物品、已知信息、情绪、动机、未完成动作、伏笔，以及本章结尾与下一章章纲的连接。

所有正文审查通过后，把正文状态改为 `awaiting_review` 并进入 `awaiting_final_review`；不得在用户终审前写成 `accepted`。

## 11. 交付与设定同步

交付整章时同时提供：

- 完整正文；
- 逻辑、文风和衔接审查摘要；
- 建议同步的设定与连续性变更；
- 额外事实追溯文件；
- 遗留风险。

交付摘要写入 `workflow/deliveries/NNNN.md`。正文首次交付或修改后再次完整交付时，正文状态保持 `awaiting_review`，流程状态为 `awaiting_final_review`，并将正文、交付摘要和流程状态创建为一次交付检查点提交。交付检查点只固定本次提交用户审阅的版本，不表示用户已经确认正文，也不授权同步设定或连续性。

修改既有设定值、推翻既有事实或增加可复用世界规则，必须先由用户确认。已确认章纲要求、且被终审正文直接确立的新增事实，可以列入交付清单。用户批准正文时，只授权同步清单中明确列出的新增事实；清单外内容不得自动写入 `canon/` 或 `continuity/`。

正文通过终审后，把正文状态改为 `accepted`，执行交付清单中获准的同步，并把 `continuity/current-state.md` 的 `through_chapter` 更新为最新连续通过且完成同步的章号。

用户直接修改已通过正文后，不自动复审、不自动同步设定、不自动提交，也不得擅自恢复旧写法；只有用户明确要求时才执行这些动作。

## 12. 发布

发布是终审之后的独立动作。用户明确要求发布时，把正文状态改为 `published`；只有 `published` 正文才允许进入项目配置的公开内容仓库。

`published` 是持续生效的发布状态，不是一次性的发布请求。已发布正文只要保持该状态，后续提交并推送的修改就会更新公开副本。不希望修改立即公开时，必须在提交前把正文改为 `awaiting_review` 或 `accepted`。

正文离开 `published` 状态时，不自动删除或下架已经公开的版本。下架必须由用户明确要求。发布同步只处理正文，不得把大纲、设定、连续性、交付摘要、旧稿或工作区文件复制到公开仓库。

项目可以通过 `workflow/publication.yaml` 配置发布目标。未启用发布或缺少目标配置时，只保留本地创作流程，不尝试同步。公开副本建立后，正文以创作仓库为唯一来源，不直接在公开内容仓库修改。

## 13. Git 规则

使用单主线工作方式，不强制创建章节分支。Git 同时记录正文交付检查点和用户确认点；两者含义不同，不得把交付检查点当作用户确认。

正文完成全部内部审查并首次或再次完整交付用户终审时，Agent 应创建交付检查点提交。用户明确确认某项产物，则授权 Agent 为该确认点创建提交，除非用户同时要求暂不提交。只允许在以下时机创建提交：

- Agent 完成正文全部审查并整章交付用户终审；
- 用户确认总纲、卷纲、单篇章纲或一批章纲；
- 用户终审通过正文；
- 用户明确要求发布或下架正文；
- 用户明确批准模板、文风指南或正式设定变更；
- 用户明确要求提交其临时修改。

Agent 内部草稿、审查和修改过程不创建提交。用户要求修改已经交付的正文时，在最近一次交付检查点之上继续工作；只有修改重新完成全部审查并再次整章交付时，才创建新的交付检查点，不改写或合并掉旧检查点。提交只包含当前检查点相关内容，不夹带无关修改。

交付检查点提交只包含状态为 `awaiting_review` 的正文、对应交付摘要和 `awaiting_final_review` 流程状态，不同步 `canon/` 或 `continuity/`。用户终审通过后的确认点提交应包含正文 `accepted` 状态、获准的设定或连续性同步、对应交付摘要和流程状态变化。其他确认点提交应包含相应产物状态变化和获准内容。

## 14. 实现边界

不得自行添加模型 API、数据库、向量检索、Word 工作流或自动决定文学内容的脚本。确需改变模板结构或实现边界时，先提交方案并等待用户确认。

子代理不是必需条件。可以自行判断是否派遣，但所有子代理必须遵守相同上下文、审查和审批规则。
